# Legacy Prototype Scripts

This directory contains the original SEOpie analyzer scripts from the pre-package prototype.

They are kept as reference material while the active implementation moves into `src/seopie_core/`.
New development should target the packaged CLI, crawler, analyzer, models, and reporters under `src/seopie_core/`.
